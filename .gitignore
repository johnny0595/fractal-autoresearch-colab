# runs.jsonl (the run history / leaderboard) IS version controlled.
# Per-run artifact dirs stay local — they're re-derivable and just clutter the repo:
runs/
run.log
dashboard.log

# Cached ground-truth eval fixtures
.cache/

# Python
__pycache__/
*.pyc
.venv/
.python-version.local

# uv
# (uv.lock IS committed, like autoresearch)
dashboard.log
